import { OrbitControls, ScrollControls } from "@react-three/drei";
import { LietuvaMap } from "./map";
import React from "react";

const MapObject = ({ children }: { children: React.ReactNode }) => {
  return <>{children}</>;
};

export default MapObject;
